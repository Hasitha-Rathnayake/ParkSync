package com.parksync.notification;

import org.springframework.stereotype.Component;

/**
 * Replaced by NotificationScheduler (payment countdowns, time-over, overstay, 15-min reminder).
 * Left as an empty component so old imports/docs still compile if referenced.
 */
@Component
public class ReminderScheduler {
    // no-op — see NotificationScheduler
}
